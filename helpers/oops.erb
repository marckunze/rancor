require 'erb'
require 'sinatra'

set :raise_errors, false
set :show_exceptions, false

error customError do
	'This is an error...' + env-['sinatra.error'].message
end

get '/' do
	raise customError, 'Click for rancor home'
end