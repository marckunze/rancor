#require 'bundler'
#Bundler.require
#require_relative 'models/models'
#require './rancor'
#require_relative 'email_helpers'

Dir.glob('lib/tasks/*.rake').each {|r| load r}


task :default do
  time1 = Time.new
  puts "Hello, it is currently: " + time1.inspect + " and rake ran."
end
